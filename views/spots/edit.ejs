<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Spot</title>
</head>

<body>
    <h1>Edit Spot</h1>
    <form action="/spots/<%=  spot._id%>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="spot[title]" value="<%= spot.title%>">
        </div>
        <div>
            <label for="location">Location</label>
            <input type="text" id="location" name="spot[location]" value="<%= spot.location%>">
        </div>
        <div>
            <label for="description">Description</label>
            <input type="text" id="description" name="spot[description]" value="<%= spot.description%>">
        </div>
        <button>Update Spot</button>
    </form>
    <a href="/spots/<%= spot._id%>">Back To Spot</a>
</body>

</html>